# L1 区策略规则（2026-07 定型版本）

> 更新时间：2026-07-17  
> 定位：CZSC 信号沉积区，不过滤，所有有买点信号的股票均进入 L1  
> 代码位置：`tmp_out/l1_score_all.py`（含 L2 EntryFilter 打分逻辑）

---

## 一、数据来源

| 数据 | 路径 | 说明 |
|------|------|------|
| 信号文件 | `data/signals/*.parquet` | 4,816 只，由 `signal_engine.py` 生成 |
| 日线数据 | `data/daily/*.parquet` | tushare pro.daily() 拉取 |
| 指数数据 | `data/index/000001.parquet` | 上证指数，用于 regime 判定 |

---

## 二、信号检测逻辑

### 检测规则

读取每个信号文件的**最后一行**（最新 K 线），检查以下 5 个信号列：

| # | 信号列 | 对应买点 |
|---|--------|:---:|
| 1 | `日线_D1B_BUY1` | 一买 |
| 2 | `日线_D1#SMA#21_BS2辅助V230320` | 二买 |
| 3 | `日线_D1#SMA#34_BS3辅助V230318` | 三买 |
| 4 | `日线_D1_三买辅助V230228` | 三买 |
| 5 | `日线_D1#SMA#34_BS3辅助V230319` | 三买 |

### 匹配优先级

按列顺序逐列检查，信号值以 `一买_` / `二买_` / `三买_` 前缀开头则为命中。命中第一列即跳出（同股只会有一个买点类型）。

### 代码逻辑

```python
for code in all_4816_stocks:
    last_row = signal_df.iloc[-1]
    for col in BUY_COLS:
        if last_row[col] starts with "一买_": → L1 (一买)
        if last_row[col] starts with "二买_": → L1 (二买)
        if last_row[col] starts with "三买_": → L1 (三买)
```

---

## 三、Regime 判定

L1 阶段即判定市场状态，用于后续 L2 打分路由。

```
上证指数 (000001.parquet)
    │
    ├─→ close > MA60 且 MA20(5日斜率) > 0 → BULL
    ├─→ close < MA60 且 MA20(5日斜率) < 0 → BEAR
    └─→ 其他 → CHOP
```

当前 regime：**CHOP**（2026-07-15，上证 3996，MA60 4088）

---

## 四、当前 L1 规模

| 买点 | 数量 | 来源 |
|------|:---:|------|
| 一买 | **954** | `日线_D1B_BUY1` |
| 二买 | **262** | BS2辅助 + BS3辅助 |
| 三买 | **144** | BS3辅助 + 三买辅助 |
| **合计** | **1,360** | / 4,816 全量 = 28% |

---

## 五、L1 不做什么

- ❌ 不过滤信号
- ❌ 不做打分
- ❌ 不做仓位管理
- ❌ 不做去重

L1 的唯一职责：**找出当前有买点信号的股票，交给 L2 打分。**

---

## 六、与 L2 的关系

| | L1 | L2 |
|---|------|------|
| 功能 | 信号沉积 | 结构打分 |
| 输入 | `data/signals/` 全量 | L1 候选池 |
| 处理 | 读最后一行，匹配前缀 | EntryFilter 多维度打分 × regime 权重 |
| 输出 | 1,360 只候选 | 179（一买）+ 5（二买）+ 10（三买）= 194 只 |
| 代码 | `tmp_out/l1_score_all.py` | `entry_filter.py` → `EntryFilter` |

---

## 七、执行命令

```bash
# 全量 L1+L2 打分（约 5 分钟）
cd /Users/hz/Desktop/Algorithmic\ Trading\ Platform
python3 tmp_out/l1_score_all.py

# 输出
tmp_out/l1_all_filtered.parquet  # L1 候选 + L2 通过标记
```

---

## 八、信号更新流程

1. 拉取最新日线：`tushare pro.daily()`
2. 删除有新数据的股票信号文件 → 强制 `signal_engine.py` 重新计算
3. 重跑 `l1_score_all.py`

（注：当前信号文件存在跳过逻辑，增量更新需手动删文件——此问题待后续自动化。）

