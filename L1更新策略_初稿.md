# L1 更新策略（初稿）

> 更新时间：2026-07-17  
> 目标：一键刷新全链路数据，从原始 K 线到 L4 排名

---

## 一、更新流水线

```
① 拉增量日线   (tushare, 上次截止日 → 今天)
    │  剔除 ST/北交所（已有日线除外）
    │
② 拉30分钟数据  (akshare, 最近30天)
    │  只拉原始K线，不跑信号
    │
③ 跑日线 CZSC 信号  (多进程8核, ~40 min)
    │  全量重生 init_n=300
    │
④ 跑 L1→L2→L3→L4  (~30 min)
    │
    └→ 输出: l4_ranked.parquet + 终端报告
```

---

## 二、各步骤详解

### ① 拉增量日线

**输入：** tushare API token  
**输出：** `data/daily/{code}.parquet`  
**耗时：** ~2 分钟  
**逻辑：**
```
1. 从 data/last_update.txt 读取上次截止日期
2. tushare pro.daily() 从上次截止日+1 → 今天
3. 逐条追加到各股票 parquet
4. 覆盖新股（stock_basic 检查新上市）
5. 更新 last_update.txt
```

**排除策略：** 目前有日线的 ST/北交所不删；新股票拉取时自动跳过 ST 和北交所。

---

### ② 拉30分钟数据（可选）

**输入：** 同 ① 后的股票列表  
**输出：** `data/min30/{code}.parquet`  
**耗时：** ~5 分钟  
**逻辑：**
```
1. akshare stock_zh_a_minute（新浪5分钟数据）
2. 最近30天数据 → 重采样为30分钟
3. 追加到已有 parquet
```

**只拉不跑信号。** 30分钟 CZSC 信号只在 L2 通过后按需生成。

---

### ③ 跑日线 CZSC 信号（核心瓶颈）

**输入：** `data/daily/{code}.parquet`  
**输出：** `data/signals/{code}.parquet`  
**耗时：** ~40 分钟（8进程）  
**脚本：** `tmp_out/build_signals_mp.py`  

**参数：**
| 参数 | 值 |
|:----|:---:|
| 进程数 | 8 (min cpu_count) |
| init_n | 300 |
| 信号频率 | 日线 (Freq.D) |
| 跳过 | 无，全量重生 |

---

### ④ L1→L2→L3→L4

**依赖：** l1_all_filtered.parquet, l3_candidates_v2.parquet  
**顺序：**
```
l1_score_all.py   → l1_all_filtered.parquet  (~30 min)
l3_filter_v2.py   → l3_candidates_v2.parquet (~5 sec)
l4_rank.py        → l4_ranked.parquet        (~30 sec)
```

---

## 三、入口命令

```bash
# 完整更新
python3 l1_update.py

# 跳过某步
python3 l1_update.py --skip-pull       # 日线已最新，跳拉取
python3 l1_update.py --skip-min30      # 不拉30分钟
python3 l1_update.py --skip-signals    # 信号不变，只跑打分
python3 l1_update.py --skip-l1l4       # 只拉数据不跑策略
```

**注：** `l1_update.py` 尚未实现，此为阶段性需求文档。

---

## 四、30分钟信号（预留规则）

30分钟 CZSC 信号**不在更新流水线中跑**，按需在 L2 通过后生成：

```
L2 通过股票 (~200只)
    │
    ├─ 检查 data/signals_30min/{code}.parquet 是否存在
    │    存在 → 跳过
    │    不存在 → 生成该股的30分钟 CZSC 信号
    │
    └→ 用于多级别联立评分（待设计）
```

---

## 五、风险

| 风险 | 影响 | 应对 |
|:---|:----|:----|
| tushare 超时/断连 | 日线拉取失败 | 加入 retry + 断点续传 |
| akshare 新浪数据不完整 | 30分钟缺失停盘日 | 不影响日线信号，可跳过 |
| CZSC 单只崩溃 | 该股无信号 | 异常隔离，跳过继续 |
| 多进程内存耗尽 | 系统卡死 | worker 数 <= cpu_count/2 |

---

## 六、日线缺口说明

当前行业分类文件（5,529 只）和 data/daily（4,991 只）差 538 只。这 538 只主要为：

| 类型 | 数量 | 处理 |
|:---|:---:|:----|
| 北交所（8xxxxx） | ~329 | 跳过，不在交易范围 |
| ST/*ST 股 | ~200 | 跳过，风险过高 |
| 其他 | ~9 | 补拉 |

**结论：缺口不补。**
