# 待开发需求清单（2026-07-17）

优先级：🔴 高 → 🟡 中 → 🟢 低

---

## 🟡 CHOP 新增 4 维度 ✅ 方案已完成

CHOP regime 下新增 4 评分维度，权重分配已确认（一买 0.50/0.35/0.20/0.15，二买 0.55/0.35/0.20/0.15，三买 0.30/0.60/0.15/0.25），旧维度同步下调 20%，待开发。

---

## 🟡 L2 按 regime 买点类型阈值调整

| 调整 | 当前 | 目标 | 理由 |
|------|:---:|:---:|------|
| BULL 一买 | 348 → **999** | 禁用 | 牛市不做一买 |
| BEAR 三买 | 277 → **999** | 禁用 | 熊市不做三买 |
| CHOP 三买 | 280 → **320** | 减少假三买 | CHOP 三买占比 55% 但胜率低 |

---

## 🟢 BULL 二买维度重设计 ✅ 方案已完成

3 个新维度替换原有失效维度（回踩比例 1.80、价量背离 0.80、趋势延续度 0.80），阈值 327 不变。

---

## 🟡 阈值与权重一致化标定

Optuna 同时优化权重和阈值，输出一致化的 (REGIME_WEIGHTS, REGIME_THRESHOLDS) 对。需要市场切换后执行。

---

## 🟡 买点分布比值交叉验证 regime

用 L1 快照的 b3/b1 比值交叉验证 regime 质量。

---

## 🟢 L1 更新策略

一次到位：拉日线 → 拉30分钟(只拉原始K线，不跑信号) → 跑日线信号 → L1-L4流水线。注：30分钟CZSC信号只在L2通过后按需生成。

---

## 🔴 进程管理规则 ✅ 已实现

AGENTS.md 已记录。

---

## 📐 设计原则

1. 一买：底部分析，极端风险厌恶
2. 二买：结构确认，空间比例最重要
3. 三买：动能延续，资金确认最重要
4. CHOP：严格止损 + 耐心持有 + 底部位置确认
5. **BULL 禁一买、BEAR 禁三买** — 逻辑优先于统计

---

## 🟡 买点分布比值交叉验证 regime（细化版）

### 1. 数据来源

| 项目 | 说明 |
|------|------|
| 输入 | L1 快照（1,360 只股票的当前买点信号） |
| b1 | L1 中一买信号数量 |
| b3 | L1 中三买信号数量 |
| 频率 | 随 L1 更新频率（signal_engine 刷新后） |
| 计算 | `ratio = b3 / max(b1, 1)`，上限 50x |

### 2. 阈值与基准

从 2022-2026 历史数据统计得出各 regime 的 b3/b1 应有范围：

| regime | 均值 | 中位数 | 合理范围 |
|:-----:|:---:|:-----:|:--------:|
| BULL | 41.6x | 13.2x | ≥ 5x |
| BEAR | 4.4x | 4.3x | ≤ 5x |
| CHOP | 12.0x | 6.1x | 3-12x |

**判定规则：**

| 当前价格 regime | b3/b1 比值 | 信号 | 动作 |
|:-------------:|:----------:|------|------|
| BULL | ≥ 5x | ✅ 一致 | 正常使用 BULL 阈值 |
| BULL | < 5x | ⚠️ 背离 | 标记"关注"，阈值偏 CHOP |
| BEAR | ≤ 5x | ✅ 一致 | 正常使用 BEAR 阈值 |
| BEAR | > 10x | ⚠️ 背离 | 标记"关注"，阈值偏 CHOP |
| CHOP | 3-12x | ✅ 一致 | 正常使用 CHOP 阈值 |
| CHOP | < 3x | ⚠️ 偏熊 | 标记"偏弱"，可考虑收紧 |
| CHOP | > 12x | ⚠️ 偏牛 | 标记"偏强"，可考虑放宽 |

**2026-07 当前状态作为样例：**

```
价格分类: BULL(70%)
b3/b1:     2.1x ← 严重背离（理论上应 ≥ 5x）
结论:      BULL 置信度低，阈值应向 CHOP 偏移
```

### 3. 实现方式（二选一）

**A. 辅助标记（推荐先做）**
- 在 L4 排名结果上方加一行状态行
- 不做自动切换，只做人工提示

```
当前 regime: BULL | b3/b1 = 2.1x | 置信度: ⚠️ 信号背离，建议关注
```

~~B. 自动联动（暂不实现）~~
- 发现背离时自动将 REGIME_THRESHOLDS 向 CHOP 偏移
- 偏移幅度：`最终阈值 = a × 当前阈值 + (1-a) × CHOP 阈值`
  - a = 置信度（= min(ratio/5, 1)）
- 需要回测验证效果

### 4. 边界情况

| 场景 | 处理 |
|------|------|
| b1 = 0（无一买） | ratio = 50x，视为 BULL 确认 |
| b3 = 0（无三买） | ratio = 0，视为 BEAR 确认 |
| 信号数据过期 > 7 天 | 标注"数据过期"，不使用该功能 |
| L1 股票数 < 500 | 样本不足，比值不可靠 |

### 5. 估值工作量

| 子任务 | 预估 |
|-------|:---:|
| 实现 b3/b1 计算（复用 L1 扫描逻辑） | 0.5 天 |
| 状态行展示（L4 输出） | 0.5 天 |
| ~~自动联动（选项 B，含回测） | 1-2 天~~ |

---


## 🔴 ExitEngine 背驰判断——30 分钟 CZSC 数据（Priority 1）

**现状**：ExitEngine 定型板背驰判断有三优先等级，Priority 1（30 分钟 CZSC）为最高优先级。当前仅有 5,745 个 30 分钟 OHLCV 文件（raw data），缺少 CZSC 构建的 bi 结构和 power 数据。

**方案**：对全部 5,745 只股票运行 30 分钟 CZSC 构建，提取 bi_list + power，存入 `data/struct_cache_30m/` 或整合到现有 struct_cache。背驰判断从 Priority 2（日线 power）升级到 Priority 1（30 分钟 CZSC）。

**工作量**：5,745 只 × ~0.2s CZSC ≈ 20 分钟（单线程，macOS 本地）。

**依赖**：无（min30 数据已有）。✅ 日线 power 已通过 `rebuild_struct_cache_power.py` 补全。
